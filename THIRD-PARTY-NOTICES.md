# Third-Party Notices

Код репозитория `ExportIFCFromRevit-CSharp` распространяется по лицензии MIT.

В папке `tools/RevitAPI/` лежат сторонние reference DLL Autodesk для локальной
сборки add-in-проектов:

- `tools/RevitAPI/2022/`
- `tools/RevitAPI/2026/`

Эти DLL не относятся к MIT-лицензии репозитория.

Этот файл нужен только как короткая памятка:

- в репозитории есть не только собственный код, но и сторонние бинарные файлы;
- условия на такие DLL нужно рассматривать отдельно от `LICENSE`;
- эти DLL лежат в репозитории как вспомогательный dev-слой для локальной сборки.
