namespace ExportIfc.Config;

/// <summary>
/// Стандартные значения переменных окружения проекта.
/// </summary>
/// <remarks>
/// Назначение:
/// 1. Разводит имена переменных окружения и их служебные значения по разным модулям.
/// 2. Делает значения межпроцессного контракта явно находимыми.
/// 3. Убирает смешение имён и значений внутри одного класса.
///
/// Контракты:
/// 1. Эти значения используются при межпроцессном обмене между оркестратором и add-in.
/// 2. Изменение значений влияет на распознавание batch-режима.
/// </remarks>
public static class EnvironmentVariableValues
{
    /// <summary>
    /// Явное значение флага autorun для пакетного запуска add-in.
    /// </summary>
    public const string AutorunEnabled = "1";

    /// <summary>
    /// Явное значение флага ожидания подключения debugger к процессу Revit.
    /// </summary>
    public const string DebugWaitAttachEnabled = "1";
}